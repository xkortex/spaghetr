FROM python:3.7 as base

COPY . /src/spaghetr

WORKDIR /src/spaghetr

RUN pip install --no-cache-dir -r build-require.txt

RUN python setup.py bdist_wheel

FROM busybox

COPY --from=base /src/spaghetr/dist /dist
